rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TOÀN QUYỀN ĐỌC/VIẾT cho tất cả collection (chỉ dùng cho test/dev)
    match /{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}

