rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Users collection - chỉ cho phép user access profile của chính họ
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Admin collection - chỉ admin có thể access
    match /admins/{adminId} {
      // Cho phép user tạo document admin của chính mình (first admin bootstrap)
      allow create: if request.auth != null && request.auth.uid == adminId;
      // Các thao tác khác yêu cầu đã là admin (tức doc của chính họ đã tồn tại)
      allow read, update, delete: if request.auth != null && 
        exists(/databases/$(database)/documents/admins/$(request.auth.uid));
    }
    
    // Menu items - Admin có thể CRUD, Users chỉ đọc
    match /menuItems/{itemId} {
      allow read: if true; // Mọi người đều có thể xem menu
      allow create, update, delete: if request.auth != null && 
        exists(/databases/$(database)/documents/admins/$(request.auth.uid));
    }
    
    // Tables - Admin có thể CRUD, Users chỉ đọc
    match /tables/{tableId} {
      allow read: if request.auth != null;
      allow create, update, delete: if request.auth != null && 
        exists(/databases/$(database)/documents/admins/$(request.auth.uid));
    }
    
    // Orders - User có thể tạo order của mình, Admin có thể xem tất cả
    match /orders/{orderId} {
      allow read: if request.auth != null && 
        (resource.data.customerId == request.auth.uid || 
         exists(/databases/$(database)/documents/admins/$(request.auth.uid)));
      
      allow create: if request.auth != null && 
        request.resource.data.customerId == request.auth.uid;
      
      allow update: if request.auth != null && 
        (resource.data.customerId == request.auth.uid || 
         exists(/databases/$(database)/documents/admins/$(request.auth.uid)));
      
      allow delete: if request.auth != null && 
        exists(/databases/$(database)/documents/admins/$(request.auth.uid));
    }
    
    // Categories - Admin có thể CRUD, Users chỉ đọc  
    match /categories/{categoryId} {
      allow read: if true;
      allow create, update, delete: if request.auth != null && 
        exists(/databases/$(database)/documents/admins/$(request.auth.uid));
    }
    
    // Restaurant settings - Chỉ admin có thể access
    match /settings/{settingId} {
      allow read, write: if request.auth != null && 
        exists(/databases/$(database)/documents/admins/$(request.auth.uid));
    }
  }
}